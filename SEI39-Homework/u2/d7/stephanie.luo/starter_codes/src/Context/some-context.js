import React from "react";

// Qn. what is the advantage of creating this file as oppose to importing createContext together with useContext directly?
const someContext = React.createContext();

export default someContext;
